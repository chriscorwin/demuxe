	<style>
        .growCircle {
            animation: growCircle 2s ease-out forwards;
            transform-origin: 102px center;
            animation-play-state: paused;
        }

        @-webkit-keyframes growCircle {
          from { r: 0; }
        }
    </style>
<div id="consent-across-channels-here" class="slds-p-bottom_xxx-small"><%- include('images/slices/consent-across-channels.svg') %></div>
<script>
	let triggeredConsentAcrossChannelsGraphAnimation = false;
	const animateConsentAcrossChannelsGraph = () => {
		if (window.scrollY > 700 && !triggeredConsentAcrossChannelsGraphAnimation) {
			triggeredConsentAcrossChannelsGraphAnimation = true;

			document.querySelectorAll('#consent-across-channels-here circle').forEach(elm => elm.style.animationPlayState = 'running');
		}

		document.addEventListener("scroll", animateConsentAcrossChannelsGraph);
	};


</script>